# ─────────────────────────────────────────────
# AI Code Reviewer - Environment Configuration
# Copy this file to .env and fill in values
# ─────────────────────────────────────────────

# App
DEBUG=false
PORT=8000

# Model Backend
# Options: "demo" | "openai" | "local"
# demo  → works immediately, no GPU needed
# openai → needs OPENAI_API_KEY below
# local  → needs GPU + fine-tuned model
MODEL_BACKEND=demo

# If MODEL_BACKEND=local, path to your fine-tuned model:
MODEL_PATH=../fine-tuning/models/code-reviewer-lora/final
BASE_MODEL_NAME=codellama/CodeLlama-7b-Instruct-hf

# If MODEL_BACKEND=openai:
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
